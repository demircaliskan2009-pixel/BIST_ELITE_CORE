# faz140: Evidence pack structure

## What changed
- `src/bist_core/cli/main.py`: Evidence in ask artifact now includes source (snapshot path) and source_sha256 when snapshot exists
- `_advice_payload`: added snapshot_source, snapshot_hash params; Evidence gets source, source_sha256
- `_fallback_payload`: same; Evidence includes source/hash when available
- `tests/test_faz140_evidence_pack_structure.py`: asserts Evidence has source and source_sha256
- `tests/test_faz139_advice_artifact_proof.py`: schema test for source/source_sha256 when present

## What tests prove it
- tests/test_faz140_evidence_pack_structure.py
- tests/test_faz139_advice_artifact_proof.py
- proof_pack green

## What to do next
- **Next phase**: faz141 (deterministic narrative format)
- Run: `.\tools\phase_commit.ps1 -Phase fazNNN -Message "..."` after implementing

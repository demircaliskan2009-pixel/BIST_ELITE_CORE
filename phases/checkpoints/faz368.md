# faz368: CLI surface golden proof

## What changed
- Added `tests/test_faz_cli_surface_golden.py`: 3 golden tests for CLI surface
  - `test_cli_surface_subcommands_preserved`: 18 top-level subcommands from `--help`
  - `test_cli_info_json_schema_preserved`: info --json keys (registry_path, datasets, symbols)
  - `test_cli_healthcheck_schema_preserved`: healthcheck checks[] with name, ok, code
- Added faz368 to `phases/backlog.yaml` (ops_tooling, refactor-safe)
- `tools/phase_commit.ps1`: optional `-RefactorProof` for ledger

## What tests prove it
- `tests/test_faz_cli_surface_golden.py` (3 tests)
- proof_pack green

## What to do next
- **Next phase**: faz135 (snapshot schema version) or faz140 (evidence pack structure)
- State: `phases/state.json` next_phase=faz135
- Run: `.\tools\phase_commit.ps1 -Phase fazNNN -Message "..."` after implementing
